Fix the following nine issues in the 
GOATflow application. Implement all 
fixes in one pass. Do not break any 
existing functionality.

═══════════════════════════════════════
FIX 1 — TIME AVAILABLE MODAL
═══════════════════════════════════════

When the user taps "Drop Into Churn Engine" 
intercept the submission and show this 
modal BEFORE processing begins.

Modal design:
Full screen overlay, z-index: 45
Background: rgba(8,8,15,0.92)
backdrop-filter: blur(8px)

Centered card:
Background: #0e0e22
Border: 1px solid rgba(124,58,237,0.4)
Border-radius: 12px
Padding: 24px 20px
Max-width: 320px

Small 🐐 emoji centered at top, 24px

Heading in Syne 700 or Helvetica-Bold, 
16px, white, centered, margin-top: 12px:
"How much time do you have right now?"

Subtext in DM Sans 400 or normal font, 
12px, #6b7280, centered, margin-top: 6px:
"GOATflow will prioritize accordingly."

Time option pills — flex row, 
flex-wrap: wrap, justify-content: center,
gap: 8px, margin-top: 16px:
"15 min" / "30 min" / "1 hour" / 
"2 hours" / "Open ended"

Each pill:
Border-radius: 20px, padding: 8px 16px
Font: 13px medium
Unselected: transparent bg, 
  border 1px solid #374151, color #6b7280
Selected: bg #7c3aed, 
  border #7c3aed, color white

After a pill is selected a 
"Let's Go →" button appears:
Background: #7c3aed, white text
Full width, border-radius: 8px
Padding: 12px, font 14px bold

Tapping "Let's Go" closes the modal 
and passes the selected time value 
as additional context to the GPT-4o 
system prompt:
"User has [X] available. 
Prioritize to fit within that window. 
Summit Calls first regardless of time."

Store selection in sessionStorage as:
'goatflow_time_available'

"Skip" link below the button:
DM Sans 400, 12px, #4b5563, centered
Skipping proceeds without time context.
Never mandatory.

═══════════════════════════════════════
FIX 2 — PER-TRACK TIME ESTIMATE
═══════════════════════════════════════

On each generated Track card add an 
optional inline time estimate selector 
below the Track description text.

Display as a collapsed single line:
"⏱ How long? →"
Font: 11px, color #4b5563
Cursor: pointer

Tapping expands inline pill row:
"5 min" / "15 min" / "30 min" / 
"1 hr" / "2+ hrs"

Same pill styling as the time modal above 
but smaller — padding: 4px 10px, 
font: 11px.

When a pill is selected:
- Store estimate against Track ID in 
  localStorage under the Track's ID
- Collapse the picker
- Show a small badge on the Track card:
  "⏱ 30 min" in muted gray, 10px
  replacing the "⏱ How long? →" text
- This estimate is used for:
  Precision Rate calculation
  Velocity Score Hay bonus
  Load Index calculation

This is fully optional — 
if never tapped the Track functions 
identically to current behavior.

═══════════════════════════════════════
FIX 3 — LOAD INDEX CALCULATION
═══════════════════════════════════════

After the Track list generates 
AND at least one Track has a 
time estimate AND time available 
was declared in the modal:

Calculate Load Index:
Sum of all Track time estimates (minutes)
÷ Time available declared (minutes)

Time available values in minutes:
15 min = 15
30 min = 30
1 hour = 60
2 hours = 120
Open ended = skip Load Index entirely

Display as a slim banner directly 
above the Track list, auto-dismisses 
after 5 seconds or on first Track tap:

Load Index > 1.5:
Background: rgba(239,68,68,0.08)
Border: 1px solid rgba(239,68,68,0.2)
Text: "⚠️ Load Index [X.X] — 
More work than time. 
Prioritizing ruthlessly."
Color: #ef4444, DM Sans 400, 12px

Load Index 0.8 to 1.5:
Background: rgba(74,222,128,0.06)
Border: 1px solid rgba(74,222,128,0.2)
Text: "✓ Load Index [X.X] — 
Good balance. You've got this."
Color: #4ade80

Load Index < 0.8:
Background: rgba(245,158,11,0.06)
Border: 1px solid rgba(245,158,11,0.2)
Text: "Load Index [X.X] — 
Light load. Real Tracks. Real Cheese. 🐐"
Color: #f59e0b

Store Load Index in sessionStorage.
Only show when both time available 
AND at least 2 Track estimates exist.

═══════════════════════════════════════
FIX 4 — PRIORITY ACCURACY FEEDBACK
═══════════════════════════════════════

After a Track is marked complete, 
show a brief feedback prompt 
below the completed Track card.

The prompt auto-dismisses after 
8 seconds if not tapped.
Always optional. Never blocks.

Design — slim card sliding down 
over 200ms below the completed Track:
Background: rgba(124,58,237,0.06)
Border: 1px solid rgba(124,58,237,0.2)
Border-radius: 8px
Padding: 8px 12px
Display: flex, align-items: center, 
gap: 10px

Left: 🐐 emoji at 14px

Center: text in DM Sans 400, 11px, #9ca3af:
"Was this ranked correctly?"

Right: two small pill buttons:
👍 "Yes" — background rgba(74,222,128,0.1),
  border rgba(74,222,128,0.3), color #4ade80
👎 "Off" — background rgba(239,68,68,0.1),
  border rgba(239,68,68,0.3), color #ef4444

Tapping "Off" reveals two sub-options 
replacing the Yes/Off buttons:
"Too high ↑" and "Too low ↓"
Same pill styling, DM Sans 400, 11px

Store all feedback in localStorage 
under 'goatflow_priority_feedback' array:
{
  trackId: string,
  trackTitle: string,
  ranked: 'correct' | 'too_high' | 'too_low',
  timestamp: Date.now(),
  activeHorns: [current Horn texts]
}

This data feeds Horn Effectiveness score.
Auto-dismiss on any tap OR after 8 seconds.

═══════════════════════════════════════
FIX 5 — BRAY CONFIRMATION MESSAGE
═══════════════════════════════════════

After voice transcription completes 
successfully and text populates 
the textarea, show a confirmation 
message above the floating Bray button.

Display as a small pill:
Text: "Bray captured. 🐐"
Font: DM Sans 500, 12px, #4ade80
Background: rgba(74,222,128,0.1)
Border: 1px solid rgba(74,222,128,0.3)
Border-radius: 20px
Padding: 4px 12px
Position: above the floating button,
right-aligned, same right edge as button

Animation:
Fades in over 200ms
Holds for 2 seconds
Fades out over 300ms
Then removes from DOM completely

This fires every time a Bray 
successfully populates text — 
not just the first time.

═══════════════════════════════════════
FIX 6 — STAGGERED TRACK ENTRANCE
═══════════════════════════════════════

Confirm and fix the staggered entrance 
animation on Track cards.

When the Track list renders after 
Churn Engine processing completes, 
each Track card must animate in 
with a cascading delay:

Card 1: animation-delay: 0ms
Card 2: animation-delay: 80ms
Card 3: animation-delay: 160ms
Card 4: animation-delay: 240ms
...each subsequent card adds 80ms

Animation per card:
@keyframes trackSlideUp {
  from { 
    opacity: 0; 
    transform: translateY(10px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
animation: trackSlideUp 300ms ease-out forwards;
animation-fill-mode: both;

Apply this dynamically in JavaScript 
when Track cards are created — 
loop through cards and set 
style.animationDelay = (index * 80) + 'ms'
on each card element.

This must fire every time a new 
Track list generates — not just 
on first load.

═══════════════════════════════════════
FIX 7 — DOCX FILE SUPPORT
═══════════════════════════════════════

Add DOCX to the accepted file types 
in the Track Sieve file upload zone.

Update the accepted file types to include:
.docx, .doc

Update the accept attribute on the 
file input element:
accept=".pdf,.png,.jpg,.jpeg,.webp,
.gif,.txt,.csv,.docx,.doc"

Update the display text in the 
file drop zone from:
"Limit 200MB per file • PDF, PNG, JPG..."
to:
"Limit 200MB per file • PDF, DOCX, PNG, JPG..."

For DOCX processing in the backend:
When a .docx or .doc file is uploaded, 
extract the text content using 
the existing document processing pipeline 
and pass the extracted text to GPT-4o 
as part of the Churn Engine input.
If the backend cannot natively parse DOCX, 
convert it to plain text by stripping 
XML tags and extracting paragraph content.

═══════════════════════════════════════
FIX 8 — MAXIMUM 10 HORNS ENFORCEMENT
═══════════════════════════════════════

Enforce a maximum of 10 active Horns 
per user.

When the user attempts to add an 
11th Horn:
- Prevent the Horn from being saved
- Show an inline message below 
  the Horn input field:
  Text: "Maximum 10 Horns reached. 
  Remove one to add another."
  Color: #f59e0b, DM Sans 400, 12px
  Fades in, persists until the 
  user deletes a Horn or dismisses

- Disable the "Add Horn" button 
  and input field visually:
  Opacity: 0.4
  Cursor: not-allowed
  Pointer-events: none

- Re-enable when Horn count drops 
  below 10

Display the current Horn count 
in the panel header alongside 
the "GOAT Horns" label:
"GOAT Horns (7/10)"
DM Sans 400, 11px, #6b7280
Updates dynamically as Horns 
are added or removed.

═══════════════════════════════════════
FINAL VERIFICATION
═══════════════════════════════════════

After implementing all 8 fixes 
confirm the following:

1. Time modal fires before every 
   Churn Engine submission without fail

2. Skip on the time modal works and 
   processing continues normally

3. Per-track estimate selector appears 
   on every generated Track card

4. Bray captured confirmation appears 
   above the floating button after 
   every successful transcription

5. Track cards animate in with 
   staggered delays every time a 
   new list generates

6. DOCX files are accepted by the 
   file upload zone

7. Priority feedback prompt appears 
   after every Track completion 
   and auto-dismisses after 8 seconds

8. Horn count displays as X/10 
   and blocks addition at 10

Do not modify any existing functionality 
not listed above.
Do not change any visual styling 
not specified above.
Report any fix that could not be 
fully implemented so I can 
address it separately.