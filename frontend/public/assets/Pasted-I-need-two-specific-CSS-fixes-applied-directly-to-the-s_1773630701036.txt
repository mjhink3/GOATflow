I need two specific CSS fixes applied 
directly to the stylesheet. Do not search 
for the problem — just apply these rules 
which will override whatever is causing 
the cropping and spacing issues.

ADD THESE RULES to the main CSS file 
or a <style> tag in the main HTML file. 
Place them at the very bottom so they 
override everything above them.

/* ═══ GOATFLOW CRITICAL OVERRIDES ═══ */

/* LOGIN LOGO — show full mascot uncropped */
.login-container,
.login-wrapper,
.login-page,
.auth-container,
.auth-wrapper,
[class*="login"],
[class*="auth"] {
  overflow: visible !important;
  clip-path: none !important;
}

.login-logo,
.login-logo img,
.auth-logo,
.auth-logo img,
[class*="logo"] img {
  width: 210px !important;
  height: auto !important;
  max-height: none !important;
  object-fit: unset !important;
  overflow: visible !important;
  display: block !important;
  margin: 8px auto 0 auto !important;
}

/* DASHBOARD BANNER — remove gap below it */
.dashboard-banner,
.mascot-banner,
.header-banner,
[class*="banner"] img,
[class*="mascot"] img {
  width: 100% !important;
  height: auto !important;
  max-height: 150px !important;
  object-fit: contain !important;
  object-position: center top !important;
  display: block !important;
  margin: 0 !important;
  padding: 0 !important;
}

/* Remove gap between banner and content */
.dashboard-content,
.main-content,
.content-wrapper,
[class*="content"],
[class*="dashboard"] {
  padding-top: 0 !important;
  margin-top: 0 !important;
}

/* Remove gap between banner container 
   and the Stateless badge below */
.banner-container + *,
.mascot-container + *,
[class*="banner"] + *,
[class*="mascot"] + * {
  margin-top: 4px !important;
  padding-top: 0 !important;
}

/* ═══ END OVERRIDES ═══ */

After adding these rules confirm:
1. The login logo shows the full mascot 
   including horns at the top
2. The dashboard banner shows the 
   goat face without cropping
3. The gap between the banner and 
   the Stateless badge is 4px or less
4. The Churn Engine button is still 
   fully visible without scrolling

If any class names above do not match 
what is in the actual codebase, 
replace them with the correct class 
names from the codebase while keeping 
all the CSS property values identical.