I am uploading two new icon images to replace 
the emoji placeholders currently used for 
Hay and Fresh Cheese throughout the app.

Save the images with these exact filenames:
- Fresh Cheese image → /public/images/icon_fresh_cheese.png
- Hay bale image → /public/images/icon_hay.png

Replace every instance of the 🌾 hay emoji 
with the icon_hay.png image.
Replace every instance of the 🧀 cheese emoji 
with the icon_fresh_cheese.png image.

Apply these replacements in every location 
they currently appear:

1. DASHBOARD STAT CARDS:
   Hay stat card: replace 🌾 with icon_hay.png
   Fresh Cheese stat card: replace 🧀 
   with icon_fresh_cheese.png
   
   Icon sizing on stat cards:
   24px × 24px
   object-fit: contain
   Vertically centered above or beside 
   the stat number
   No background color on the img element — 
   the black in the image blends naturally 
   with the dark card background

2. SIDE PANEL — MY STATS SECTION:
   Hay Balance row: replace 🌾 with icon_hay.png
   Fresh Cheese Banked row: replace 🧀 
   with icon_fresh_cheese.png
   
   Icon sizing in side panel stats:
   18px × 18px
   object-fit: contain
   Vertically centered with the stat label text

3. GOATIFICATIONS SIDE PANEL TOGGLES:
   Speed Bonus toggle: replace 🌾 
   with icon_hay.png at 14px × 14px
   Fresh Cheese Earned toggle: replace 🧀 
   with icon_fresh_cheese.png at 14px × 14px

4. IN-APP TOAST NOTIFICATIONS:
   Any Goatification toast that references 
   Hay or Fresh Cheese — replace the emoji 
   in the toast body text with the 
   respective icon at 16px × 16px 
   inline with the text
   Use vertical-align: middle on the img 
   so it sits flush with the text baseline

5. HAY TO FRESH CHEESE CONVERSION ANIMATION:
   The conversion celebration animation 
   that fires when 500 Hay converts — 
   replace both emojis in the sequence 
   with the icon images:
   icon_hay.png → icon_fresh_cheese.png
   Display at 48px × 48px during the 
   animation for visual impact

6. TUTORIAL CARD 4 — HAY AND FRESH CHEESE:
   Replace 🌾 and 🧀 emojis in the 
   tutorial card content with the 
   respective icons at 32px × 32px
   centered above their respective 
   explanatory text blocks

7. PASTURE GAUGE BAR:
   The "X/500 Hay to next 🧀" label 
   at the bottom of the screen — 
   replace 🧀 with icon_fresh_cheese.png 
   at 14px × 14px inline
   Use vertical-align: middle

8. MASCOT CALLOUT BOX ON TUTORIAL CARD 4:
   The "Real Tracks. Real Cheese. 🧀" 
   closing line — replace 🧀 with 
   icon_fresh_cheese.png at 16px × 16px inline

GLOBAL RULE:
After these replacements, no 🌾 or 🧀 
emoji should remain anywhere in the 
user-facing UI. 
Search the entire codebase for both 
emoji characters and confirm all 
instances have been replaced.

Do not replace these emojis in:
- Code comments
- Console logs  
- Any backend or API references
- The goatflow_notif_prefs localStorage keys
Only replace user-facing UI text and 
rendered HTML/JSX elements.

IMAGE RENDERING NOTE:
Both images have black backgrounds that 
blend naturally with the app's dark theme.
Do not add border-radius, box-shadow, 
or any container styling to these images — 
let them sit directly in the UI with 
their natural black backgrounds.
The only exception is the stat cards 
where a very subtle 4px border-radius 
on the img element is acceptable 
if it improves visual consistency 
with surrounding card elements.