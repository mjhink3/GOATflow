Fix the spacing issues on the login page 
and main dashboard. These are the only 
changes needed — do not modify any other 
styling, functionality, or content.

═══════════════════════════════════════
LOGIN PAGE SPACING FIX
═══════════════════════════════════════

There is a large empty gap between the 
top of the screen and where the logo begins.

Fix as follows:

1. Find the main login container or 
   wrapper element and set:
   padding-top: 0
   margin-top: 0

2. Find the logo/mascot image element 
   and set:
   margin-top: 12px
   This puts the logo close to the 
   top of the viewport with minimal 
   breathing room only.

3. Reduce the logo image size on mobile:
   max-height: 200px
   width: auto
   object-fit: contain
   This prevents the logo from consuming 
   too much vertical space.

4. Reduce spacing between elements:
   Logo to tagline gap: 8px
   Tagline to subtitle gap: 6px  
   Subtitle to Login/Create tabs gap: 12px
   Tabs to form gap: 8px
   Between form fields: 8px
   Login button margin-top: 10px

5. The goal: on a 667px tall mobile screen 
   the user should see:
   - Logo (top, compact)
   - Tagline
   - Subtitle  
   - Login/Create Account tabs
   - Username field
   - Password field
   - Login button
   - Footer
   All visible without scrolling.
   Nothing cut off. No dead space.

═══════════════════════════════════════
DASHBOARD SPACING FIX
═══════════════════════════════════════

There is a large empty black gap between 
the top navigation bar (where >> and ⋮ sit) 
and where the mascot banner begins.
This gap is pushing all content down 
and preventing the full dashboard 
from being visible above the fold.

Fix as follows:

1. Find the element creating the gap 
   above the mascot banner — 
   check for any of these:
   - margin-top or padding-top on the 
     main content container
   - A spacer or placeholder div above 
     the banner
   - A min-height on the nav bar 
     creating space below it
   - A top property on the banner element
   - Any CSS that positions content 
     below a fixed header with offset

2. Set the mascot banner to sit 
   flush immediately below the nav bar:
   margin-top: 0
   padding-top: 0
   top: 0 (if absolutely positioned)

3. The mascot banner height:
   Mobile: 80px fixed
   object-fit: cover
   object-position: center 38%
   This shows the eyebrow, smirk, 
   glasses, and tie — 
   the full personality of the mascot 
   in a slim strip.

4. After the banner reduce all 
   section gaps on mobile to minimum:
   Banner to Stateless badge: 6px
   Stateless badge to Track Sieve label: 6px
   Track Sieve label to file drop zone: 4px
   File drop zone height: 80px mobile
   File drop zone to paste area: 6px
   Paste area height: 70px mobile
   Paste area to Churn Engine button: 8px
   Churn Engine button height: 48px
   Churn Engine button to stat cards: 8px

5. Stat cards 2x4 grid:
   Each card height: 60px
   Gap between cards: 6px
   4 rows × (60px + 6px) - 6px = 258px

6. TOTAL BUDGET CHECK on 667px screen:
   Nav bar: 44px
   Banner: 80px
   Badge + label: 50px
   File drop zone: 80px
   Gap: 6px
   Paste area: 70px
   Gap: 8px
   Churn Engine button: 48px
   Gap: 8px
   Stat cards: 258px
   Pasture Gauge: 44px (fixed bottom)
   Total: ~696px

   If this exceeds the screen height 
   reduce in this order:
   First: reduce file drop zone to 70px
   Then: reduce paste area to 60px
   Then: reduce stat card height to 56px
   Stop reducing when total fits in 620px 
   (leaving 44px for Pasture Gauge 
   and 3px breathing room).

7. The Churn Engine button must always 
   be fully visible without scrolling 
   on a 375x667px screen.
   This is non-negotiable.
   If nothing else fits, keep this button.

═══════════════════════════════════════
TWO ADDITIONAL SMALL FIXES
═══════════════════════════════════════

FIX A — Login subtitle copy update:
Find the subtitle text that currently reads:
"GOATflow metabolizes chaotic inputs into 
prioritized operational action and evolves 
with you."

Replace with:
"Nothing that matters gets lost.
Nothing that doesn't matter gets in the way."

This is two lines. Display each as a 
separate line, centered, in the same 
green color (#3DAA6A or current green).
This is the product promise in its 
sharpest form and belongs on the 
login screen directly beneath the tagline.

FIX B — Churn Engine button styling:
The current Churn Engine button appears 
to have a mountain/terrain graphic inside it 
which adds visual busyness to a button 
that should be clean and prominent.

Replace the button background with:
Solid purple #7c3aed — no texture, 
no terrain pattern, no gradient.
White text, bold, with the ⚡ emoji prefix.
Subtle glow on the button:
box-shadow: 0 0 20px rgba(124,58,237,0.5)

The button should be the most visually 
prominent element below the input zone — 
a clean confident purple rectangle 
that is impossible to miss.

═══════════════════════════════════════
DO NOT CHANGE
═══════════════════════════════════════
- The mascot banner image itself
- The terrain background or tier labels
- The Bray floating button position
- The Pasture Gauge content or styling
- The stat card data or icon assignments
- The Track card design or content
- Any functionality whatsoever
- Desktop layout — mobile only changes